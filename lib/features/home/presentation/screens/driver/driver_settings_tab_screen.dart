import 'package:flutter/material.dart';
import 'package:car_app/features/home/presentation/screens/driver/widgets/driver_settings_tab_view.dart';

// ─────────────────────────────────────────────────────────────────────────────
// Driver Settings Tab Screen
// ─────────────────────────────────────────────────────────────────────────────
class DriverSettingsTabScreen extends StatelessWidget {
  const DriverSettingsTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const DriverSettingsTabView();
  }
}
