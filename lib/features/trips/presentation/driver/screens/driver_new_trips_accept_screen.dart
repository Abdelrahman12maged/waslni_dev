import 'package:flutter/material.dart';
import 'package:car_app/features/trips/presentation/driver/widgets/new_trips/driver_new_trips_accept_view.dart';

// ─────────────────────────────────────────────────────────────────────────────
// Driver New Trips Accept Screen
// ─────────────────────────────────────────────────────────────────────────────
class DriverNewTripsAcceptScreenClean extends StatelessWidget {
  const DriverNewTripsAcceptScreenClean({super.key});

  @override
  Widget build(BuildContext context) {
    return const DriverNewTripsAcceptView();
  }
}
