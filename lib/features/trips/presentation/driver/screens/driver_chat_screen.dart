import 'package:flutter/material.dart';
import 'package:car_app/features/trips/presentation/driver/screens/widgets/driver_chat_view.dart';

// ─────────────────────────────────────────────────────────────────────────────
// Driver Chat Screen (Entry Point)
// ─────────────────────────────────────────────────────────────────────────────
class DriverChatScreenClean extends StatelessWidget {
  const DriverChatScreenClean({super.key});

  @override
  Widget build(BuildContext context) {
    return const DriverChatView();
  }
}
